name: contacts_example

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

  contacts_service: ^0.4.6
  permission_handler: ^5.0.1+1

  # firebase 
  firebase_core: ^0.5.0
  cloud_firestore: ^0.14.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/